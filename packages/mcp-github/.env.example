# mcp-github — package-level env template (safe to commit)
# Values here override the repo-root .env. Most setups only need the root .env.

# GitHub
GITHUB_TOKEN=
GITHUB_REPO=your-org/your-repo

# Jira (needed to create remote links on tickets)
JIRA_BASE_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=
JIRA_PO_PROJECT_KEY=PO
JIRA_DEV_PROJECT_KEY=DEV

MCP_GITHUB_HTTP_PORT=4002
