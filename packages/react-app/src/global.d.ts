// Build-time constants injected by Vite `define` (see vite.config.ts).
// __APP_VERSION__ comes from packages/react-app/package.json "version" — a single
// source of truth for the header version pill (v1.13, P0 — replaces a hardcoded label).
declare const __APP_VERSION__: string;
