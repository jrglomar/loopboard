# Dependencies
node_modules/

# Build output
dist/
coverage/

# Environment — never commit credentials (spec §4.4)
.env
.env.*
!.env.example
# v1.65: the negation above only covered .env.example — this template was silently
# ignored (and thus never committed) since it was created. It holds placeholders only.
!.env.docker.example

# Leaves/offset + team roster stores (v1.5/v1.8 — host-local JSON, never committed)
.invokeboard-leaves.json
*.invokeboard-leaves.json
.invokeboard-team.json
*.invokeboard-team.json
# Huddle impediments + pending-PR stores (v1.16 — host-local JSON, never committed)
.invokeboard-impediments.json
*.invokeboard-impediments.json
.invokeboard-prs.json
*.invokeboard-prs.json
# Huddle post-scrum + meeting-goal stores (v1.20 — host-local JSON, never committed)
.invokeboard-post-scrum.json
*.invokeboard-post-scrum.json
.invokeboard-meeting-goal.json
*.invokeboard-meeting-goal.json
# Offset-ledger store (v1.26 — host-local JSON, never committed)
.invokeboard-offset.json
*.invokeboard-offset.json
# Huddle meeting-notes store (v1.41 — host-local JSON, never committed)
.invokeboard-meeting-notes.json
*.invokeboard-meeting-notes.json
# Retro store (v1.42 — host-local JSON, never committed)
.invokeboard-retro.json
*.invokeboard-retro.json
# Task Helper user store (v1.44 — accounts + ENCRYPTED connections, NEVER committed)
.invokeboard-users.json
*.invokeboard-users.json
# Per-user store dir (v1.45 — each user's leaves/team/retro/etc. JSON, NEVER committed)
.invokeboard-user-stores/
# Crash-leftover atomic-write temp files (v1.63 — writeJsonAtomic's same-dir *.tmp before rename)
*.tmp

# Editor / OS
.DS_Store
*.local
.idea/
